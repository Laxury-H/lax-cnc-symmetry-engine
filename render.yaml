services:
  - type: web
    name: lax-cnc-symmetry-engine
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn src.web.app:app --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.7
    plan: free
